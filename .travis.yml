language:
    - objective-c

install:
    - brew install cmake
    - brew install ninja
    - time brew install qt5

script:
    - cmake --version
    - pwd
    - ls
    - mkdir build
    - cd build
    - cmake -G Ninja ..
    - ninja
